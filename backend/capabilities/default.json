{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Default capability for the main Scribe window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
